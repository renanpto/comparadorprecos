{
  "name": "orcafacil-backend",
  "version": "1.0.0",
  "private": true,
  "description": "OrçaFácil AI — backend Lambda (AWS SAM)",
  "type": "commonjs",
  "scripts": {
    "build": "sam build",
    "deploy:dev": "sam deploy --config-env dev",
    "deploy:prod": "sam deploy --config-env prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.906.0",
    "@aws-sdk/client-dynamodb": "^3.906.0",
    "@aws-sdk/client-s3": "^3.906.0",
    "@aws-sdk/lib-dynamodb": "^3.906.0",
    "@aws-sdk/s3-request-presigner": "^3.906.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
